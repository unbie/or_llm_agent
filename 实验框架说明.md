# OR-LLM-Agent 实验框架完整说明文档

> **创建时间**: 2026-04-01  
> **项目**: 基于LLM的运筹优化问题求解框架 - ALNS算法求解生鲜配送VRP  
> **作者**: 上海交通大学 & 南洋理工大学

---

## 📋 目录

1. [框架总体架构](#框架总体架构)
2. [核心模块说明](#核心模块说明)
3. [实验类型对比](#实验类型对比)
4. [文件关系图](#文件关系图)
5. [实验流程详解](#实验流程详解)
6. [数据流转过程](#数据流转过程)
7. [使用场景指南](#使用场景指南)

---

## 🏗️ 框架总体架构

### 三层架构设计

```
┌─────────────────────────────────────────────────────────────────┐
│                         应用层 (Application)                      │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │ run_batch_no_llm │  │ experiment_*_mgr │  │ or_llm_eval  │  │
│  │   (批量实验)      │  │   (实验管理器)    │  │  (LLM评估)   │  │
│  └──────────────────┘  └──────────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         核心层 (Core Engine)                      │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │heuristic_skeleton│  │ HeuristicPlugin  │  │    utils     │  │
│  │  (ALNS框架)      │  │   (算子库)        │  │ (成本计算)   │  │
│  └──────────────────┘  └──────────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         数据层 (Data)                             │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │ Solomon Benchmark│  │ Experiment Results│  │   Analysis   │  │
│  │   (输入数据)      │  │   (结果JSON)      │  │  (图表报告)  │  │
│  └──────────────────┘  └──────────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 核心设计理念

1. **分层解耦**: 应用层、核心层、数据层职责分离
2. **可插拔算子**: HeuristicPlugin支持LLM生成或手工编写
3. **多模式运行**: 支持无LLM、单次LLM、批量LLM三种模式
4. **标准化评估**: 统一的结果格式和分析工具

---

## 📁 核心模块说明

### 一、核心求解引擎 (3个文件)

#### 1. `heuristic_skeleton.py` - ALNS算法框架
**作用**: 自适应大邻域搜索(ALNS)算法的完整实现

**核心功能**:
```python
class HeuristicSolver:
    - 初始解生成: nearest_neighbor() + random_init()
    - ALNS迭代优化: solve()
    - 算子权重自适应: adaptive_weight_update()
    - 成本计算: calculate_total_cost()
    - 可行性检查: check_feasibility()
```

**关键特性**:
- ✅ 标准ALNS框架 (Ropke & Pisinger, 2006)
- ✅ 3个破坏算子 + 2个修复算子
- ✅ 模拟退火接受准则
- ✅ 算子权重自适应调整
- ✅ 生鲜物流多维成本计算

**输入**: 
- `data`: 问题数据 (顾客、车辆、时间窗等)
- `plugin`: HeuristicPlugin实例 (提供算子)

**输出**: 
- 最优解方案
- 成本历史记录
- 算子使用统计

---

#### 2. `utils.py` - 成本计算工具
**作用**: 生鲜物流特有的多维成本计算

**核心类**:
```python
class FreshnessAndPenaltyCalculator:
    - C11: 车辆固定成本 (240元/辆)
    - C12: 距离成本 (3元/公里)
    - C13: 制冷成本 (15元/小时)
    - C2:  货损成本 (基于新鲜度函数)
    - C3:  时间窗惩罚 (早到/晚到)
```

**成本公式**:
```
总成本 = C11 + C12 + C13 + C2 + C3

其中:
- C11 = 车辆数 × 240
- C12 = 总距离 × 3
- C13 = 行驶时长 × 15
- C2  = Σ p·Di·max(ri - δ1, 0)  # 新鲜度损失
- C3  = Σ fi(t)  # 时间窗惩罚
```

**与标准VRPTW的区别**:
| 维度 | 标准VRPTW | 生鲜物流VRP |
|------|-----------|-------------|
| 目标 | 最小化距离 | 最小化综合成本 |
| 约束 | 容量+时间窗 | 容量+时间窗+新鲜度 |
| 成本维度 | 1维 (距离) | 5维 (距离+时间+货损+...) |

---

#### 3. `heuristic_prompts.py` - LLM提示词模板
**作用**: 用于LLM生成HeuristicPlugin算子的提示词

**包含内容**:
- ALNS算法原理说明
- VRP问题描述
- 算子设计规范
- 示例代码模板

**使用场景**: 仅在LLM模式下使用 (方案二)

---

### 二、实验管理系统 (4个文件)

#### 4. `run_batch_no_llm.py` - 批量实验脚本 ⭐
**作用**: **零Token成本**的批量实验运行器 (你当前使用的)

**核心逻辑**:
```python
1. 定义实验配置:
   experiments = [
       ('c1', 'c101.txt', 3),  # 数据集类型, 文件名, 重复次数
       ('c1', 'c105.txt', 3),
       ...
   ]

2. 使用预定义的FIXED_PLUGIN_CODE:
   - random_removal: 随机移除算子
   - route_removal: 路径移除算子
   - string_removal: 连续节点移除算子
   - greedy_insert: 贪心插入算子
   - regret_insert: 后悔插入算子

3. 执行流程:
   for 每个数据集:
       for 每次重复:
           - 加载数据集
           - 创建HeuristicPlugin实例
           - 调用HeuristicSolver.solve()
           - 保存结果到JSON
```

**优势**:
- ✅ 不消耗LLM Token
- ✅ 运行速度快 (2-3分钟/实例)
- ✅ 适合算法验证和论文初稿

**输出**:
```
experiments_batch/results/
  ├── c1_c101_run1.json
  ├── c1_c101_run2.json
  ├── c1_c101_run3.json
  └── ...
```

---

#### 5. `run_batch_quick_test.py` - 快速测试脚本
**作用**: `run_batch_no_llm.py`的简化版，用于快速验证

**区别**:
- 更少的数据集 (1-3个)
- 更少的重复次数 (1次)
- 用于调试和验证环境

---

#### 6. `experiment_quick_manager.py` - 快速实验管理器
**作用**: 轻量级实验配置和管理

**功能**:
- 实验配置生成
- 数据集路径管理
- 结果收集和初步分析

---

#### 7. `experiment_heuristic_manager.py` - 完整实验管理器
**作用**: 高级实验管理系统，支持复杂场景

**核心类**:
```python
class HeuristicExperimentConfig:
    - 实验名称、模型、数据集
    - 超参数 (温度、迭代次数、破坏比例)
    - 是否使用LLM生成算子

class HeuristicExperimentManager:
    - 生成实验配置
    - 执行实验批次
    - 结果汇总和分析
    - 图表生成
```

**支持的实验类型**:
1. 模型对比实验 (不同LLM模型)
2. 消融实验 (LLM vs Baseline)
3. 超参数调优实验
4. 稳定性实验

---

### 三、LLM评估模块 (2个文件)

#### 8. `or_llm_eval.py` - LLM求解器评估 (基础版)
**作用**: 使用LLM完整求解OR问题 (建模+编程+求解)

**流程**:
```
问题描述 (NLP)
    ↓
LLM建模 → 数学模型 (LaTeX)
    ↓
LLM编程 → 求解代码 (Python/Gurobi)
    ↓
执行求解 → 最优解
    ↓
结果评估 → 准确率统计
```

**使用场景**: 
- 完整的OR问题求解 (不限于VRP)
- 评估LLM的建模能力
- 与BWOR数据集配合使用

---

#### 9. `or_llm_eval_async_resilient.py` - 异步容错版
**作用**: `or_llm_eval.py`的增强版

**改进**:
- ✅ 异步并发执行 (提升速度)
- ✅ 错误容错和重试机制
- ✅ 断点续传 (中断后可恢复)
- ✅ 更详细的日志记录

**适用场景**: 大规模LLM评估实验

---

### 四、结果分析工具 (2个文件)

#### 10. `result_analyzer_batch.py` - 批量实验结果分析器 ⭐
**作用**: 分析`run_batch_no_llm.py`生成的结果

**分析内容**:
```python
1. 总体统计:
   - 成功率
   - 平均成本
   - 平均车辆数
   - 平均耗时

2. 按数据集类型分组:
   - C1, C2, R1, R2, RC1, RC2
   - 每类的平均性能

3. 按实例分组:
   - 每个实例的稳定性分析
   - 变异系数 (CV)

4. 与Solomon BKS对比:
   - Gap计算
   - 车辆数对比

5. 可视化图表:
   - 性能对比图
   - 稳定性箱线图
   - 计算时间分布图
```

**输出文件**:
```
experiments_batch/
├── tables/
│   ├── table1_dataset_comparison.csv    # 数据集类型对比
│   ├── table2_instance_details.csv      # 实例详情
│   └── table3_stability_analysis.csv    # 稳定性分析
├── figures/
│   ├── fig1_dataset_comparison.png      # 性能对比图
│   ├── fig2_stability_boxplot.png       # 稳定性箱线图
│   └── fig3_computation_time.png        # 计算时间图
└── latex/
    ├── table1.tex                        # LaTeX表格
    ├── table2.tex
    └── table3.tex
```

**关键指标**:
- **Gap to BKS**: ((你的成本 - 最优成本) / 最优成本) × 100%
- **CV (变异系数)**: (标准差 / 平均值) × 100%
- **成功率**: 可行解数量 / 总实验数

---

#### 11. `result_analyzer_heuristic.py` - 启发式实验结果分析器
**作用**: 分析LLM生成算子的实验结果

**额外分析**:
- LLM vs Baseline对比
- 不同模型性能对比
- 超参数影响分析
- 收敛曲线对比

---

## 🔄 实验类型对比

### 方案一: 无LLM批量实验 (当前使用)

| 维度 | 说明 |
|------|------|
| **执行脚本** | `run_batch_no_llm.py` |
| **Token消耗** | **0** (不调用LLM) |
| **算子来源** | 预定义的FIXED_PLUGIN_CODE |
| **实验数量** | 12数据集 × 3重复 = 36次 |
| **耗时** | 2-3小时 |
| **适用场景** | 快速验证算法、论文初稿、算法对比 |
| **优势** | 快速、免费、稳定 |
| **劣势** | 无法展示LLM生成能力 |

### 方案二: LLM生成实验

| 维度 | 说明 |
|------|------|
| **执行脚本** | `experiment_heuristic_manager.py` |
| **Token消耗** | 高 (每次调用LLM生成算子) |
| **算子来源** | LLM实时生成 |
| **实验数量** | 可配置 (通常50-200次) |
| **耗时** | 5-20小时 |
| **适用场景** | 论文完整实验、模型对比、消融实验 |
| **优势** | 展示LLM能力、更全面的评估 |
| **劣势** | 成本高、时间长 |

### 方案三: LLM完整求解

| 维度 | 说明 |
|------|------|
| **执行脚本** | `or_llm_eval_async_resilient.py` |
| **Token消耗** | 极高 (建模+编程) |
| **问题类型** | 通用OR问题 (不限VRP) |
| **数据集** | BWOR / IndustryOR / MAMO |
| **适用场景** | 评估LLM建模能力 |

---

## 🔗 文件关系图

### 调用关系

```
┌─────────────────────────────────────────────────────────────────┐
│                      用户入口                                     │
└─────────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ↓                   ↓                   ↓
    ┌─────────┐        ┌─────────┐        ┌─────────┐
    │ 方案一   │        │ 方案二   │        │ 方案三   │
    │ batch_no│        │heuristic│        │or_llm   │
    │ _llm.py │        │_mgr.py  │        │_eval.py │
    └─────────┘        └─────────┘        └─────────┘
          │                   │                   │
          └───────────────────┼───────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      核心引擎层                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  heuristic_skeleton.py (ALNS框架)                        │  │
│  │    ├─ HeuristicSolver.solve()                           │  │
│  │    ├─ HeuristicSolver.calculate_total_cost()            │  │
│  │    └─ HeuristicSolver.check_feasibility()               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↓                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  HeuristicPlugin (算子库)                                 │  │
│  │    ├─ random_removal()      ├─ greedy_insert()          │  │
│  │    ├─ route_removal()       └─ regret_insert()          │  │
│  │    └─ string_removal()                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↓                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  utils.py (成本计算)                                      │  │
│  │    └─ FreshnessAndPenaltyCalculator                      │  │
│  │         ├─ C11 (固定成本)                                 │  │
│  │         ├─ C12 (距离成本)                                 │  │
│  │         ├─ C13 (制冷成本)                                 │  │
│  │         ├─ C2  (货损成本)                                 │  │
│  │         └─ C3  (惩罚成本)                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      结果分析层                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  result_analyzer_batch.py                                │  │
│  │    ├─ load_results()                                     │  │
│  │    ├─ analyze_results()                                  │  │
│  │    ├─ generate_tables()                                  │  │
│  │    └─ generate_figures()                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 数据依赖关系

```
数据集
  └─ data/1 Solomon Benchmark/
       ├─ c1/c101.txt
       ├─ c2/c201.txt
       └─ ...
              ↓
        [读取和解析]
              ↓
    问题数据 (JSON/Dict)
      {
        "customers": [...],
        "vehicle_capacity": 200,
        "vehicle_fixed_cost": 240,
        ...
      }
              ↓
        [HeuristicSolver]
              ↓
    实验结果 (JSON)
      {
        "exp_name": "c1_c101_run1",
        "best_cost": 85009.06,
        "num_routes": 21,
        ...
      }
              ↓
    [result_analyzer]
              ↓
  分析报告 (CSV/PNG/LaTeX)
    ├─ tables/
    ├─ figures/
    └─ latex/
```

---

## 📊 实验流程详解

### 流程1: 无LLM批量实验 (你当前使用的)

```
[开始]
  ↓
1. 运行脚本: python run_batch_no_llm.py
  ↓
2. 加载实验配置
   experiments = [
     ('c1', 'c101.txt', 3),  # 12个数据集, 每个3次重复
     ...
   ]
  ↓
3. 循环执行 (36次):
   ├─ 3.1 加载数据集
   │    └─ parse_solomon_file('data/1 Solomon Benchmark/c1/c101.txt')
   │
   ├─ 3.2 创建算子插件
   │    └─ exec(FIXED_PLUGIN_CODE)  # 预定义的算子代码
   │         ├─ random_removal
   │         ├─ route_removal
   │         ├─ string_removal
   │         ├─ greedy_insert
   │         └─ regret_insert
   │
   ├─ 3.3 创建求解器
   │    └─ HeuristicSolver(data, plugin)
   │
   ├─ 3.4 执行求解
   │    └─ solver.solve(max_iterations=1000)
   │         ├─ 生成初始解
   │         ├─ ALNS迭代优化
   │         │    ├─ 选择破坏算子
   │         │    ├─ 移除节点
   │         │    ├─ 选择修复算子
   │         │    ├─ 重新插入
   │         │    ├─ 计算成本
   │         │    ├─ 接受准则判断
   │         │    └─ 更新权重
   │         └─ 返回最优解
   │
   └─ 3.5 保存结果
        └─ save_json('experiments_batch/results/c1_c101_run1.json')
             {
               "exp_name": "c1_c101_run1",
               "dataset_type": "c1",
               "instance": "c101",
               "run_id": 1,
               "best_cost": 85009.06,
               "num_routes": 21,
               "elapsed_time": 145.64,
               "success": true
             }
  ↓
4. 所有实验完成
   └─ 生成 36 个 JSON 文件
  ↓
5. 运行分析器: python result_analyzer_batch.py
  ↓
6. 生成报告
   ├─ tables/table1_dataset_comparison.csv
   ├─ figures/fig1_dataset_comparison.png
   └─ latex/table1.tex
  ↓
[结束]
```

### 流程2: LLM生成实验

```
[开始]
  ↓
1. 配置实验
   └─ HeuristicExperimentConfig
        ├─ model: "deepseek-r1"
        ├─ dataset: "c101"
        └─ use_plugin_generation: True
  ↓
2. 调用LLM生成算子
   ├─ 发送prompt (包含ALNS原理、VRP描述)
   ├─ LLM生成HeuristicPlugin代码
   └─ 验证代码语法
  ↓
3. 执行求解 (同流程1的3.3-3.5)
  ↓
4. 对比分析
   └─ LLM生成 vs Baseline对比
  ↓
[结束]
```

---

## 🌊 数据流转过程

### 完整数据流

```
┌─────────────────┐
│ Solomon数据集    │ (原始格式: .txt)
│ c101.txt        │
└────────┬────────┘
         ↓
    [parse_solomon_file()]
         ↓
┌─────────────────┐
│ 标准化数据字典   │
│ {               │
│   customers: [  │
│     {id, x, y,  │
│      demand,    │
│      ready_time,│
│      due_date,  │
│      ...}       │
│   ],            │
│   vehicle_cap,  │
│   params        │
│ }               │
└────────┬────────┘
         ↓
    [HeuristicSolver]
         ↓
┌─────────────────┐
│ 解方案          │
│ [               │
│   [0,5,3,7,0],  │  # 路径1
│   [0,2,8,0],    │  # 路径2
│   ...           │
│ ]               │
└────────┬────────┘
         ↓
    [FreshnessAndPenaltyCalculator]
         ↓
┌─────────────────┐
│ 成本分解        │
│ {               │
│   C11: 5040,    │  # 固定成本
│   C12: 25000,   │  # 距离成本
│   C13: 10000,   │  # 制冷成本
│   C2:  30000,   │  # 货损成本
│   C3:  15000,   │  # 惩罚成本
│   total: 85040  │
│ }               │
└────────┬────────┘
         ↓
    [保存结果]
         ↓
┌─────────────────┐
│ 实验结果JSON    │
│ {               │
│   exp_name,     │
│   best_cost,    │
│   num_routes,   │
│   elapsed_time, │
│   ...           │
│ }               │
└────────┬────────┘
         ↓
    [result_analyzer]
         ↓
┌─────────────────┐
│ 分析报告        │
│ ├─ CSV表格     │
│ ├─ PNG图表     │
│ └─ LaTeX代码   │
└─────────────────┘
```

---

## 🎯 使用场景指南

### 场景1: 快速验证算法性能 (推荐新手)

```bash
# 1. 运行小规模测试 (5分钟)
python run_batch_quick_test.py

# 2. 查看结果
ls experiments_batch/results/

# 3. 分析结果
python result_analyzer_batch.py
```

**适用于**: 
- 算法调试
- 环境验证
- 快速原型

---

### 场景2: 生成论文数据 (你当前场景)

```bash
# 1. 运行完整批量实验 (2-3小时)
python run_batch_no_llm.py

# 2. 分析结果
python result_analyzer_batch.py

# 3. 获得论文素材
# - experiments_batch/tables/*.csv (表格数据)
# - experiments_batch/figures/*.png (图表)
# - experiments_batch/latex/*.tex (LaTeX代码)
```

**适用于**:
- 论文初稿
- 算法对比
- 稳定性验证

---

### 场景3: LLM能力评估 (高级)

```bash
# 1. 配置实验
# 编辑 experiment_heuristic_manager.py

# 2. 运行LLM实验
python experiment_heuristic_manager.py --model deepseek-r1

# 3. 对比分析
python result_analyzer_heuristic.py
```

**适用于**:
- 模型对比
- 消融实验
- 论文完整版

---

### 场景4: 完整OR问题求解

```bash
# 使用LLM完整求解OR问题
python or_llm_eval_async_resilient.py \
  --model deepseek-r1 \
  --dataset data/datasets/BWOR.json
```

**适用于**:
- 通用OR问题
- LLM建模能力评估

---

## 📊 当前实验状态分析

### 你的实验配置

```python
实验类型: 方案一 - 无LLM批量实验
脚本: run_batch_no_llm.py
数据集: 12个Solomon实例
重复次数: 每个3次
总实验数: 36次
状态: ✅ 已完成 (36/36)
```

### 初步结果

```
成功率: 1/36 (2.8%)  ⚠️ 异常低!
成功实例: c1_c101_run1
  - 成本: 85009.06
  - 车辆数: 21
  - 耗时: 145.6秒
  - 车辆数Gap: +110% (vs BKS的10辆)
```

### 问题诊断

从成功率仅2.8%来看，可能存在以下问题:

1. **模块导入错误**: 之前的实验有`ModuleNotFoundError: No module named 'utils'`
2. **临时文件路径**: 代码在临时目录执行，可能找不到utils模块
3. **编码问题**: 文件读写编码问题

**建议**: 检查失败的35个实验的错误日志

---

## 🔍 下一步行动建议

### 立即执行

1. **安装缺失依赖**:
```bash
pip install jinja2  # 用于LaTeX导出
```

2. **重新运行分析器**:
```bash
python result_analyzer_batch.py
```

3. **检查失败原因**:
```bash
# 查看失败实验的详细错误
python -c "
import json
from pathlib import Path
for f in Path('experiments_batch/results').glob('*.json'):
    data = json.load(open(f))
    if not data.get('success'):
        print(f'{f.name}: {data.get(\"output\", \"\")[:200]}')
"
```

### 中期目标

1. 修复实验问题 (utils模块导入)
2. 重新运行完整实验
3. 分析对比结果
4. 撰写论文素材

### 长期目标

1. 运行LLM生成实验 (方案二)
2. 多模型对比
3. 超参数调优
4. 完整论文实验

---

## 📚 参考文献

1. Ropke, S., & Pisinger, D. (2006). An adaptive large neighborhood search heuristic for the pickup and delivery problem with time windows. *Transportation Science*, 40(4), 455-472.

2. Solomon, M. M. (1987). Algorithms for the vehicle routing and scheduling problems with time window constraints. *Operations Research*, 35(2), 254-265.

3. 项目论文: Zhang, B., & Luo, P. (2025). OR-LLM-Agent: Automating modeling and solving of operations research optimization problem with reasoning large language model. *arXiv preprint arXiv:2503.10009*.

---

## 📞 联系方式

- **GitHub**: https://github.com/bwz96sco/or_llm_agent
- **arXiv**: https://arxiv.org/abs/2503.10009
- **HuggingFace**: https://huggingface.co/datasets/SJTU/BWOR

---

**文档结束**
